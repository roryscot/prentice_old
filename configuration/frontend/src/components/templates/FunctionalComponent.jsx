import React, { Component } from 'react';
import PropTypes from 'prop-types';

const ComponentName = (props) => {
    return (
        <div>
            {"content"}
        </div>
    );
};

ComponentName.protoTypes = {};

export default ComponentName;